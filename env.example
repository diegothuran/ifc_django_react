# Configurações do Django IFC Monitoring

# ==================== DJANGO CORE ====================

# Chave secreta do Django (OBRIGATÓRIA)
# Gere uma nova chave com: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here

# Modo debug (NUNCA use True em produção!)
DEBUG=False

# Hosts permitidos (separados por vírgula)
ALLOWED_HOSTS=localhost,127.0.0.1,.onrender.com,your-domain.com

# ==================== DATABASE ====================

# URL do banco de dados PostgreSQL
# Formato: postgresql://user:password@host:port/database_name
# Para desenvolvimento local com SQLite, deixe comentado
DATABASE_URL=postgresql://user:password@host:5432/database_name

# ==================== CACHE E REDIS ====================

# URL do Redis para cache e Celery
# Formato: redis://host:port/db
# Exemplo local: redis://localhost:6379/0
# Exemplo produção: redis://user:password@host:port/db
REDIS_URL=redis://localhost:6379/0

# ==================== SEGURANÇA ====================

# Origens permitidas para CORS (separadas por vírgula)
# Exemplo: https://app.example.com,https://admin.example.com
CORS_ALLOWED_ORIGINS=

# ==================== SENTRY (Monitoramento de Erros) ====================

# DSN do Sentry para tracking de erros
# Obtenha em: https://sentry.io/settings/your-project/keys/
SENTRY_DSN=

# Ambiente do Sentry (development, staging, production)
SENTRY_ENVIRONMENT=production

# ==================== CELERY (Tarefas Assíncronas) ====================

# Celery usa o REDIS_URL configurado acima
# Não é necessário configuração adicional

# ==================== EMAIL (Opcional) ====================

# Backend de email
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Configurações SMTP (se usar email real)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# ==================== RENDER DEPLOY ====================

# Essas variáveis são configuradas automaticamente no Render
# WEB_CONCURRENCY=4
# PYTHON_VERSION=3.11.10
# RENDER_GIT_COMMIT=git-commit-hash

# ==================== ARQUIVOS ESTÁTICOS E MÍDIA ====================

MEDIA_URL=/media/
STATIC_URL=/static/
